package com.example.android.bakingapp.ViewModel;

import android.app.Application;
import android.arch.lifecycle.AndroidViewModel;
import android.arch.lifecycle.LiveData;
import android.support.annotation.NonNull;

import com.example.android.bakingapp.database.AppDatabase;

import java.util.List;

public class QuantityModel { //extends AndroidViewModel {
/*
    private LiveData<List<String>> quantity ;

    public QuantityModel(@NonNull Application application) {
        super(application);

        AppDatabase database = AppDatabase.getInstance(application);
        quantity = database.taskDao().getQua();
    }


    public LiveData<List<String>> getQuantity() {
        return quantity;
    }
    */
}
