package com.example.android.bakingapp.ViewModel;

import android.app.Application;
import android.arch.lifecycle.AndroidViewModel;
import android.arch.lifecycle.LiveData;
import android.support.annotation.NonNull;

import com.example.android.bakingapp.database.AppDatabase;
import com.example.android.bakingapp.database.AppExecutor;

import java.util.List;

public class NameModel {//extends AndroidViewModel {
/*
    private LiveData<List<String>> getName ;

    public NameModel(@NonNull Application application) {
        super(application);

        AppDatabase database = AppDatabase.getInstance(application);
        if (database.taskDao().getName() != null)
        getName = database.taskDao().getName();
    }


    public LiveData<List<String>> getName() {
        return getName;
    }
    */
}
